CI failed. Analyze the errors below and produce a plan to fix them.

```
{{ciOutput}}
```

## Instructions
1. Read the failing test/build files to understand what went wrong.
2. Identify the root cause -- is it a logic error, missing import, wrong type, missing dependency?
3. Produce a minimal fix plan. Do not refactor or add features -- just fix the failures.

## Output Format

### Root Cause
1-2 sentences: what went wrong.

### Fix Plan
Numbered steps. Each step: file, what to change, why.
